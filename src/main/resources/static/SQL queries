--CREATE TABLE users (
    username VARCHAR(50) PRIMARY KEY,
    access_level VARCHAR(20),
    fields TEXT,
    password VARCHAR(255) NOT NULL
);


---CREATE TABLE shipment_details (
       id VARCHAR(36) PRIMARY KEY DEFAULT (UUID()),
       commodity VARCHAR(255),
       port_of_loading VARCHAR(255),
       destination VARCHAR(255),
       icd VARCHAR(255),
       forwarder VARCHAR(255),
       shipping_line VARCHAR(255),
       booking_number VARCHAR(255),
       seal_number VARCHAR(255),
       empty_container_received VARCHAR(255),
       weight DECIMAL(19,2),
       loading_status VARCHAR(255),
       vgm_filled VARCHAR(255),
       custom_clearance VARCHAR(255),
       shipping_bill_filled VARCHAR(255),
       bae_received VARCHAR(255),
       container_status VARCHAR(255),
       bl_instruction_filled VARCHAR(255),
       draft_generated VARCHAR(255),
       bl_draft_status VARCHAR(255),
       bill_landing_number INT,
       loading_pics_shared BOOLEAN,
       freight_paid BOOLEAN,
       material_status VARCHAR(255),
       supplier_name VARCHAR(255),
       supplier_lme_percentage DOUBLE,
       supplier_lme_rate DOUBLE,
       supplier_final_rate DOUBLE,
       supplier_invoice_value DOUBLE,
       supplier_payment_terms VARCHAR(255),
       supplier_payment_date DATETIME,
       supplier_claim BOOLEAN,
       supplier_claim_settled BOOLEAN,
       supplier_claim_amount DOUBLE,
       buyer_name VARCHAR(255),
       consignee_name VARCHAR(255),
       quality_of_material VARCHAR(255),
       container_no VARCHAR(255),
       sample_report_available BOOLEAN,
       bl_draft VARCHAR(255),
       loading_photos_shared BOOLEAN,
       buyer_lme_percentage DOUBLE,
       lme_fixed_status VARCHAR(255),
       invoice_generated BOOLEAN,
       invoice_date DATETIME,
       buyer_lme_rate DOUBLE,
       buyer_final_rate DOUBLE,
       buyer_invoice_value DOUBLE,
       amount_credited DOUBLE,
       balance DOUBLE,
       additional_charges DOUBLE,
       discount DOUBLE,
       sales_invoice_shared BOOLEAN,
       payment_credit_date DATETIME,
       expected_date_of_departure DATETIME,
       bl_courier BOOLEAN,
       bl_requirement BOOLEAN,
       expected_date_of_arrival DATETIME,
       buyer_claim_shortage BOOLEAN,
       buyer_claim_settled BOOLEAN,
       buyer_claim_amount DOUBLE,
       payment_credited BOOLEAN,
       created_date DATETIME,
       updated_date DATETIME,
       updated_by VARCHAR(255)
   );


--INSERT INTO users (username, access_level, fields, password)
VALUES ('test@gmail.com','admin', '[ "commodity", "forwarder", "bookingNumber", "emptyContainerReceived", "icd", "destination", "portOfLoading", "buyerClaimAmount", "supplierLmePercentage" ]','123456');